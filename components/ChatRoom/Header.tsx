import React from 'react'

type Props = {}

const Header = (props: Props) => {
  return (
    <div className={`w-full h-[12dvh] border border-black`}>
      Header
    </div>
  )
}

export default Header